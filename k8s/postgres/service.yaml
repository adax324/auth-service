apiVersion: v1
kind: Service
metadata:
  name: postgres-auth-headless
  namespace: driving-school
  labels:
    app: postgres-auth
spec:
  clusterIP: None
  selector:
    app: postgres-auth
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
  namespace: driving-school
  labels:
    app: postgres-auth
spec:
  selector:
    app: postgres-auth
  ports:
    - port: 5432
      targetPort: 5432
